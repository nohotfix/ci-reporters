{
  "name": "@nohotfix/example-vitest-app",
  "version": "0.0.0",
  "private": true,
  "description": "Dogfood: a Vitest v3 suite reporting into NoHotfix via @nohotfix/vitest-reporter (no curl).",
  "type": "module",
  "scripts": {
    "dogfood": "vitest run"
  },
  "dependencies": {
    "@nohotfix/vitest-reporter": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
