{
  "name": "@nohotfix/example-cypress-app",
  "version": "0.0.0",
  "private": true,
  "description": "Dogfood: a Cypress suite reporting into NoHotfix via @nohotfix/cypress-reporter (no curl).",
  "scripts": {
    "dogfood": "cypress run"
  },
  "dependencies": {
    "@nohotfix/cypress-reporter": "workspace:*"
  },
  "devDependencies": {
    "cypress": "^13.17.0"
  }
}
