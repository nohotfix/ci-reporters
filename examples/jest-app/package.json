{
  "name": "@nohotfix/example-jest-app",
  "version": "0.0.0",
  "private": true,
  "description": "Dogfood: a Jest suite reporting into NoHotfix via @nohotfix/jest-reporter (no curl).",
  "scripts": {
    "dogfood": "jest"
  },
  "dependencies": {
    "@nohotfix/jest-reporter": "workspace:*"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
